{
  "dependencies": {
    "react": "^17.0.2",
    "react-dom": "^17.0.2",
    "webcash": "^1.0.3"
  },
  "devDependencies": {
    "@parcel/transformer-less": "^2.4.0",
    "buffer": "^6.0.3",
    "events": "^3.3.0",
    "parcel": "^2.4.0",
    "process": "^0.11.10",
    "stream-browserify": "^3.0.0"
  },
  "scripts": {
    "serve": "parcel src/index.html",
    "build-github":   "rm -rf dist/github   && parcel build src/index.html --target github",
    "build-firebase": "rm -rf dist/firebase && parcel build src/index.html --target firebase",
    "build-bundle":   "rm -rf dist/bundle   && parcel build src/index.html --target bundle"
  },
  "targets": {
    "github": {
      "publicUrl": "./",
      "sourceMap": false
    },
    "firebase": {
      "sourceMap": false
    },
    "bundle": {
      "publicUrl": "./",
      "sourceMap": false
    }
  }
}
